{
    "name": "civiclink-root",
    "version": "1.0.0",
    "description": "CivicLink Root",
    "scripts": {
        "dev:frontend": "npm --prefix frontend run dev",
        "dev:server": "npm --prefix server run dev",
        "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:server\"",
        "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm install --prefix server --include=dev && npm run build --prefix server",
        "start": "node server/dist/index.js",
        "start:emulators": "echo 'Emulators deprecated. Use npm run dev to start local server.'"
    },
    "devDependencies": {
        "@vercel/node": "^5.5.14",
        "concurrently": "^8.2.2"
    }
}